# API Keys Policy

All third-party keys (including `PAYMENTS_API_KEY`) are stored in the secrets
vault and injected at deploy time — never committed to source.

This policy is owned by the team that operates the [[payments-service]].
