# Payments Service

The payments-service authorizes and captures charges for checkout.

Production incidents are escalated per the [[oncall-runbook]]. The API
credentials it consumes are issued and rotated under the [[api-keys-policy]].
